# Tinycmdr 1.0.0 — handoff 2026-09-23 (session d): rounds 8-10, the batch dupe, the notes race, the stolen answer

Continues `docs/handoff-2026-09-23c.md`. Three drive rounds on the Windows test box (WO8/9/10), two bug
fixes, one polish, all pushed to the test box and proven live. The other five hosts are untouched
(the fleet is the operator's call at release, per standing rule).

```
repo        C:/Users/<user>\tinycmdr (the manager box; the repo IS the install), branch main
HEAD        779596a  drive round 9: a composed answer is delivered even when steering arrives with it
            0eb6e3f  drive round 8: a batch runs a call once, and the notes rewrite waits its turn
VERSION     1.0.0  UNPUBLISHED. No bump: owed at the release.
tree now    tinycmdr.py 9091f78fe1747fd0 + draft-drop polish (final hash = fleet-version-report)
            tinycmdr-cli.py regenerated after every edit in the same pass
gates       sweep-f2f3.sh -> 51 runs SWEEP_FAIL=0 (twice; final run after the draft-drop line)
            tests/test_ledger_race.py 29 (was 22) both builds · tests/test_stall.py 255 (was 252)
            bot build only · tests/test_checkin.py 120 green after the polish
the Windows test box   pushed twice (18:58 build 5de76325, 19:20 build 9091f78f) + one polish push after
            this file's gate; restarted each time through the bot's own /restart (downtime 3-4s,
            `posted startup notice to <id>` on record)
```

## 1. What the rounds found (each fix cites its measurement)

```
WO8   one 7-call batch (4 remember + 2 task add + 1 shell) + one 3-call batch.
      The SECOND batch came back a,b,c,a,b,c: six calls, byte-identical args digests
      (args_len 20/20 pairs), EVERY copy executed, loop guard warned 3x. A doubled
      `remember`/`task add` doubles its side effect.
      FIX (0eb6e3f): a batch runs the FIRST copy of an identical (name, raw-args)
      pair; later copies answer "NOT EXECUTED: ... already in THIS batch (position
      N)" and count as refused. `refused` matches both harness refusal shapes.
      PROOF (WO10c/WO9): identical args_len=20 pair -> results 1350 then 218 chars
      (the suppression text) - one execution.
      ROOT CAUSE of the model's doubled batch: NOT resolved (model repetition vs
      the MTP stream). The harness now defends both ways; re-open only if the
      suppression log ("duplicate within one batch suppressed") grows.

WO8   notes.md was the ledger race on another file: `remember` appended under no
      lock and then ran curate_notes - a whole-file read-modify-write that saved
      with a PLAIN open("w"). The drive's four facts landed in write order 1,3,4,2
      (concurrent writers, safe by luck). record_authored_note was already locked.
      FIX: curate_notes' body runs under the notes.md path lock (_curate_notes_impl),
      its save is atomic_write_text, tool_remember + tool_notes wear
      serialized_on(NOTES_FILE). Gate: test_ledger_race 22 -> 29 (8 parallel
      remembers all land; both tools BLOCK while the lock is held). FALSIFIED: the
      pre-fix build fails exactly the two lock checks.

WO9   THE STOLEN ANSWER. The model composed the full task answer (1647 streamed
      chunks); STEER-1 landed in the same second; "steering from david arrived with
      the answer — taking another turn" ran, and the run's DELIVERED response
      carried only the steering reply. The composed answer survived ONLY as the
      leftover streamed draft post (its text lives in the post's attachment PROPS -
      a first read of the empty "message" field misreported it as erased) and no
      lane treats draft residue as a delivery; CLI/web lanes have no drafts at all
      and lose it outright.
      FIX (779596a): the composed answer is DELIVERED through say_cb before the
      steering turn (lanes with no say_cb carry it with the final return); the
      steering message no longer claims it "overrides the earlier instruction"
      (steering is often additive and that wording made the model abandon finished
      work); the streamed draft is dropped (narration_drop_cb) once the answer is
      posted properly.
      PROOF (WO10c, live): 408-chunk answer stream -> POST 19:31:11.066 (the say
      delivery, "WO10C ANSWER: 29229" + 60 padding lines) -> "arrived with the
      answer — taking another turn" 2ms later -> "STEER-10 acknowledged." at
      19:31:15.6. BOTH posts in the channel (the say post is a BAR post: message
      empty, text in props - see the traps).

WO10  side observations (measured once each, not chased):
      - ORDER PICKUP LATENCY 12-79s: two of three orders waited 60-80s and were
        picked up by the 60s catch-up sweep, not the websocket. Measure again
        before touching anything.
      - A run answered "Get-Date" from the CARRY (run 20's result 4 minutes old)
        and skipped the requested shell call entirely (WO10b). The random-number
        variant (WO10c) forced a real call. Claim discipline is prompt-level.
      - The model's account of its OWN earlier run contradicted the journal (the
        WO9 draft says "call c was search_files ... exactly what I ran. No
        substitution"; the journal measured 2x execute_code(print("hermes-agent"))
        and search_files only in a later turn). A model's self-report is a CLAIM.
      - OUTAGE PATH (operator's .47 restart window): stream broke -> non-stream
        retry -> clean "infra: no LLM endpoint answered: 503" and the run ended
        with nothing changed. Working as designed.
```

## 2. Also fixed in the same pass (open item 4, test infra)

Per-suite staging: test_ledger/test_ledger_race/test_stall left the shared
`%TEMP%\tinycmdr-test-stage` for `tinycmdr-test-stage-<name>` dirs (two parallel sweeps had
produced 4 phantom test_cli failures). The sweep's console-spec list now also grades
test_ledger_race on the CLI build. One flake class killed in the new lock tests: a thread that
had not STARTED yet passed "blocks" for the wrong reason under load - a start handshake
(`entered` Event) plus 10s completion margins; 21 straight green runs since.

## 3. Fleet state

```
the Windows test box   a LAN address  the test box: carries every fix above. Its .bak-push wo8/wo9
                        backups are cleaned; OLDER .bak-push20260923-* from earlier
                        sessions remain (litter rule - sweep with clean-install.ps1 -DryRun)
the rest    the manager box tree is the reference; .9/.13/.47/.3 still run 8a7f5bef from session c.
                        The fleet push is the operator's call at release.
config      .20 runs llm.allow_cloud_fallback=false (its startup line warns every run)
                        while the fleet standard is true since 2026-09-16. NOT touched -
                        if it is deliberate for the test box, say so and it goes in the notes.
```

## 4. How to work on it (same kit as handoff c, plus)

```
drive       python bin/mm_say.py --to 6600 --file <order.txt>          (announce on)
            steering: same, or --no-announce for a bare verb (/restart works this way
            and the bot records + announces it itself - the operator-visible door)
late-steer repro (the WO10c driver): ONE script posts the order AND watches the
            events journal AND fires the steer when the shell call lands (+2.5s) -
            a separate watcher round-trip is too slow (runs finish in 15-30s):
            hermes-tmp/wo10c-driver.py
grading     mm_dump SKIPS bar posts (message empty) - say-delivered answers are
            invisible there; read the channel posts with props attachments
            (the API probe in the WO10c notes) or the journal + log
gates       sweep-f2f3.sh alone (51 runs); test_stall is bot-build-only on purpose
falsify     git show <pre-fix-commit>:tinycmdr.py > probe-pre.py; TINYCMDR_SRC=probe-pre.py
            python tests/<suite>.py  - the new checks must FAIL there, not crash
push        backup+copy+hash over //a LAN address/C$/tinycmdr, then bare /restart via
            mm_say --no-announce; verify `connected to Mattermost as @6600` +
            `posted startup notice to <id> (downtime Ns)`
```

## 5. Traps this session paid for

```
- A Mattermost BAR post keeps its text in props.attachments and has message="".
  Reading only "message" misreported a live answer as an erased post - and the
  claim nearly shipped. Always fetch props.
- heredoc byte literals: the JSON decode halves backslashes - a b'... \\' anchor
  arrives as \' and python reads an unterminated string. Build backslashes with
  chr(92).
- The idle check: the log grows every 60s (the catch-up poll) - the RUN JOURNAL's
  mtime is the idleness signal, not the log's.
- A timing test needs a thread-start handshake; "blocked" alone passes for the
  wrong reason and flakes under sweep load.
- delta-equality newline checks are wrong for LF inserts into mixed files: compare
  added-crlf against the replacement's crlf, added-lf against its lf.
- test_stall subclasses MattermostDispatcher: it cannot run against the console
  build (AttributeError at import) - that is why it is not in the CLI spec list.
```

## 6. The operator's standing asks, restated

```
- Every fix above cites a measurement (WO8's digests, the notes write order, WO9's
  delivered-response gap, WO10c's two posts).
- Prompt lines touched this session: the steering turn's user message (delivered-
  already wording, quoted in the commit).
- One version per batch; bytes are never urgent; the restart is his call (exercised
  per round in this loop by his instruction).
- Never restart a host with an active run. Never message a bot about its own lifecycle.
- Public posts: no IPs, no hostnames, no creds.
```
