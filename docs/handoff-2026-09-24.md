# Tinycmdr 1.0.0 - handoff 2026-09-24 (efficiency review): the payload is 32% lighter and the skills load 74% less

(Replaces handoff-2026-09-23d.md; the efficiency plan it executed is DONE and deleted with this
commit - git keeps both: `git log --oneline -- docs/`.)

```
repo        C:/Users/<user>\tinycmdr (the manager box; the repo IS the install), branch main
HEAD        0626710  efficiency 4: experiment on-demand, websocket liveness watchdog, prune_events wired
            5609125  efficiency review done: docs chain folded into handoff-2026-09-24
            1a632bd  efficiency 2+3: prompt lines trimmed (operator-approved); dormant debris deleted
            e6b2100  efficiency 1: parked skills stop renting the prompt; gate prints payload floor
VERSION     1.0.0  UNPUBLISHED. No bump: owed at the release.
            FLEET IN SYNC (2026-09-23 night): all six hosts in sync on build d382f5e0b8aac173
            (includes Mac launchd /restart fix & Mattermost draft-reuse fix).
measured    system prompt   20,942 ch / ~6,159 est-tok  ->  14,244 ch / ~4,189 est-tok  (-32%)
            tool schemas    8,518 ch / ~2,505 est-tok   ->  7,346 ch / ~2,160 est-tok   (-14%)
            turn floor      the Windows test box peak prompt_tokens 14.5K  ->  13.0K (0-step turn, verified)
            skills loaded   tinycmdr-build-release 99.8K -> 26.4K, fleet-access 52.9K -> 24.0K
                            (121K of stories moved VERBATIM to references/, lazy via skill_view)
            payload floor   build-package.py prints it per build: fixture-staged prompt+schemas
                            9,898 + 7,346 = 17,244 ch (~5,071 est-tok) · build 27c89590463174d4
gates       sweep-f2f3.sh 51 runs SWEEP_FAIL=0 (after each of the two code batches)
            falsify: pre-fix build fails exactly "a skill in a parked dot dir never reaches
            the index"; every suite-pinned prompt clause survived the trim (24 pins checked)
drive       WO11 green on the Windows test box: pickup 0.3s, 5 steps/4 calls, narration fired as trimmed,
            find_tools -> search_files by name, write+read-back verified, terse 3-result report
```

## The three things this pass proved (keep them)

```
- A dot dir under skills/ is PARKED and never indexed. The 76 runbooks in
  skills/.imported-unused (deleted 2026-09-23 by operator call) were 5,442 of the prompt's
  20,942 chars on every call. The cut was DATA, not prompt lines: +0 prompt bytes.
- The (measured: ...) story tails in prompt lines are rent. 25 bullets trimmed to their
  rules and short tags; every rule and every pinned clause intact. The full diff:
  `git show 1a632bd` (operator-approved before the fact as "Trim").
- Skill file rule going forward: one lesson per line; a section that has not been true for
  a week goes to references/ or is deleted; SKILL.md <=30KB. Enforced by this rewrite.
```

## Open items the review ranked but did not build (the queue)

```
latent    E CARRY CAP: tool_carry_chars=8000. Measure refetch-saved from the `carry:` log
            lines (54 renderings across two boxes), then tune down, not off. Leave alone
            for now until more multi-turn data accumulates.
          F (Scan class remainder): "name the door" on the 3rd status-shaped shell call.
            tools/later.py investigation in .20's provenance.
resolved  D (0626710): experiment moved on-demand (-1,170 ch schemas). ask_user kept visible.
          F (0626710): ws_connect heartbeat=30s + receive_timeout=60s; 15m dead listener watchdog
            exits 75 for instant supervisor self-heal.
          I (0626710): prune_events() wired into _RunSpan.__exit__; log rotation active.
optional  instrumentation: evidence check / "now callable" / symbol map fire into result
            text and log NOWHERE (grep proves 0 everywhere) - +3 log lines, 0 prompt bytes,
            and the next dormancy review gets real firing counts.
          comment/docstring narration in tinycmdr.py (134KB, 16%): costs tokens only when
            the model reads its own build. Measure self-read frequency first (the re-read
            ledger counts "text bought from X").
          4 CLI-build scripts (82KB) could be 2. Maintainer-side only.
resolved   2026-09-23 (operator's calls): .20 llm.allow_cloud_fallback=false is DELIBERATE
            for the test box (recorded in fleet-access's llm block); skills/.imported-unused
            and 14 one-shot maintenance scripts DELETED; prompt trim approved as "Trim".
firing     mechanism counts measured 2026-09-23 (the manager box 24h / .20 24h): route hint 153/3,
counts     loop guard 203/22, batch dupe 174/4, carry render 30/24, notes guard 24/1,
            steering 0/22. The result-text mechanisms are unlogged (see optional).
```

## Traps this pass paid for (2026-09-23/24)

```
- A fuzzy patch tool on tinycmdr.py (CRLF) silently rewrote an unrelated 464-line region
  while ast.parse passed. Edit that file with bytes.replace; `cp tinycmdr.py probe-pre.py`
  first (one-second undo AND the falsify build). Check `git diff --stat` after every edit.
- tinycmdr-cli.py regen is TWO passes: build-cli-source.py THEN build-cli-fix.py. Stage 1
  alone looks complete and silently drops the console blocks (the skill already warned).
- build-cli-fix.py anchors 7 console prompt subs on exact bot-build bullet text: a prompt
  trim must re-point those subs in the same commit or the fix pass REFUSES (by design).
- Mattermost has no /usage verb (that is Telegram): the run's finish line carries it
  ("Done - N step(s) ... peak N tok ctx") and the run journal keeps run.end - that is the
  completion signal to poll, not file mtime.
- BAR posts hold their text in props.attachments and update IN PLACE (create_at is the
  draft's, update_at is the real finish). Always fetch props and sort on update_at.
```

## How to work on it (same kit)

```
drive       python C:/Users/David Trapp/bin/mm_say.py --to 6600 --file <order.txt>
            (Windows paths for native python! /c/Users/... fails)
            steering/verbs: same with --no-announce (bare /restart is the operator door)
            driver with pickup/run.end timing + props-aware grading: hermes-tmp/wo11-driver.py
gates       bash ~/hermes-tmp/sweep-f2f3.sh   (51 runs, both builds; logs to sweep-f2f3.log)
falsify     cp tinycmdr.py probe-pre.py before an edit; TINYCMDR_SRC=probe-pre.py
            python tests/<suite>.py - the new checks must FAIL there, not crash
push (.20)  backup+copy+hash over //a LAN address/C$/tinycmdr, bare /restart via mm_say
            --no-announce, verify `connected to Mattermost as @6600` + `posted startup
            notice to <id> (downtime Ns)`
```

## The operator's standing asks, restated

```
- Every fix cites its measurement; a prompt line needs a measured miss, a rejected runtime
  alternative, and its token rent stated in the commit. Fix with ZERO prompt bytes first.
- One version per batch; bytes are never urgent; the restart and the fleet push are his
  call (the push waits for the public GitHub release).
- Never restart a host with an active run. Never message a bot about its own lifecycle.
- Public posts: no IPs, no hostnames, no creds. Posts amended, never replaced.
- He reviews prompt line diffs. Skill/trap knowledge goes in skills (or docs/dev-log.md),
  never in the bot's prompt-surface files (notes.md, field-notes.md, atlas.md).
