# Tinycmdr 1.0.0 — handoff 2026-09-23 (later session): the operator drive, four fix rounds, the toolsmith

A checkpoint for picking this up in a new conversation. It says where the keys live, not what they are.
The earlier handoff (F1/F2/F3, the tool-visibility batch) is `docs/handoff-2026-09-23.md` and is now
DONE — this file continues from it.

```
repo        C:/Users/<user>\tinycmdr (the manager box; the repo IS the install), branch main
HEAD        b3536b2  toolsmith: the shipped tool that makes, imports and indexes drop-in tools
            bccda53  drive round 4: a bare question about a call that never ran must not cost 20 min
            0b7ca06  drive round 3: the doors a miss should name, and claims that are not measurements
            cc5d641  route hint: log it when it fires
            cc78afc  route hint: a shell content search is pointed at search_files, once per run
            8db4fcc  handoff: F1/F2/F3 done
            69f66d0  F2/F3: the hidden-tool inventory line + the check-must-test-the-claim clause
VERSION     1.0.0  UNPUBLISHED. No bump: a bump is owed at the release, not per edit.
tree now    tinycmdr.py 4c66ada89721cdc9 · tinycmdr-cli.py 0ced4960a7f4e438 ·
            tinycmdr-supervise.py 200ead14c7909da3
gates       ~/hermes-tmp/sweep-f2f3.sh -> 40 suites + 8 console-build specs, SWEEP_FAIL=0
            tests/test_tool_discovery.py 80 (79 on the CLI) · tests/test_route_hint.py 29 ·
            tests/test_toolsmith.py 28 · everything else unchanged and green
```

## 1. What is deployed (and what is not)

The operator said "restart all" and it was done: **all five reachable hosts restarted through their own
doors with the notice armed**, each announcing itself ("Back up — tinycmdr v1.0.0, listening again (Ns …)").

```
at the 13:31 push    the other Windows box .9, the Windows test box .20, the Linux test box .13, the LAN model box .47, the manager box
                     tinycmdr.py 3ac47605769f599b (the F2/F3 build) + cli 9bdaf4f4543e2433
the Windows test box .20        then restarted twice more on its own (the test box): a330952b0b528036
                     = 0b7ca06, the round-3 fixes. It does NOT carry bccda53 or the toolsmith.
the other four       still run 3ac47605 (F2/F3). the manager box's tree is b3536b2 and its bot runs an older
                     process (bytes matter, not the process, until the next restart).
tools/ on .9/.20/.47 patch.py + process.py + README.md pushed, load-probed per host with that
                     host's own python; .13 already had the two .py and got the README.
MacBook .3           ASLEEP / unreachable the whole session. It still needs the code batch AND
                     the three starter tools (then toolsmith) when it wakes.
dist/                untouched: the 1.0.0 kit predates the starter tools, so a rebuild is owed at
                     the next real cut (never rebuild dist/ under a published/validated number).
```

**First move of the next session:** push the tree (tinycmdr.py + tinycmdr-cli.py) plus
`tools/toolsmith.py` to the five hosts, restart the idle ones (never one with an active run), and
read `fleet-version-report.ps1` back. Order and scripts: section 5.

## 2. The drive: method, and what it found

Method (first-class, and reusable): **drive the box as the operator.**
`python ~/bin/mm_say.py --to 6600 --file <order.txt>` posts as @david and announces the real sender;
the bot's own log (`\\a LAN address\C$\tinycmdr\tinycmdr.log`) and its event journal
(`sessions/mm-<id>.events.jsonl`) are readable over SMB, so every answer is
graded against the tree and against the tool calls that actually ran.

```
WO1  three probes: what is NOT in your tool list; every atomic_write_text call site with line
     numbers; the fuzzy-anchor tool + a real edit.
WO2  the same call-site question; config.json vs config.example.json; a background job reported
     without blocking.
WO3  a multi-file content search; a write whose payload matches a confirm pattern; a fresh-eyes
     sub-agent re-check of WO2's answer.
WO4  the multi-file search again; re-check the sub-agent's claims yourself and say which; quote
     the last web_search result.
```

Findings, all fixed and gated (the measurements are the reason each fix exists):

```
1  the SHELL route for file work, still. "find every line that calls X" = Select-String + a second
   Select-String for the defs + a python regex in execute_code + a 13,482-char spill + a repeat-read
   map: 6 calls and 4.5 minutes for what ONE search_files call answers, and search_files was never
   called. The hidden tool's NAME is in the prompt and that did not change it: the model reaches for
   the shell verb it knows cold over a tool whose argument SHAPE it has never seen (the payload
   budget is 8,518 of the 8,900 gate, so search_files' 528-char schema does not fit).
   FIX: route_hint() - one line on the shell result the miss already cost, naming the call
   (search_files {pattern, path}, pattern is a regex). Fires at most TWICE per run (once was not
   enough: the model repeated the same Select-String four minutes later), stands down once
   search_files is in the session's payload or when disclosure is off, never fires on a grep over
   COMMAND output, and logs itself so a drive can grade it. Plus the routing bullet now names what
   search_files replaces (Select-String, findstr, grep, rg) and carries its call shape.
2  the inventory line's tail said "the custom tools further down work the same way", and the model
   read the 7 hidden CORE tools as CUSTOM tools. Reworded ("Those are core tools; ...").
3  asked for its tools, the run padded the answer with a 105-name skill taxonomy. Prompt: a skill is
   a runbook, not a tool.
4  a hidden tool's shape, asked of the WRONG door: skill{read, name: search_files} answered with
   1.7 KB of skill names; the model also typed tool names into the SHELL (list_tools, then
   'list_tools 2>&1 | Select-String …') and read the PowerShell error as a failed tool.
   FIX: the skill tool answers "<x> is a TOOL on this box, not a skill" AND carries that tool's
   arguments (so the miss costs no second hop); the shell answers the same for a bare tool name or a
   pipeline whose first token is one.
5  the run reported "search_files errored with a 512 on the glob" and had NEVER called the tool.
   FIX: prompt - only a TOOL RESULT proves a tool ran; never report an output you did not receive.
6  a verifier SUB-AGENT invented an eighth config difference (agent._stream_notes — verified
   byte-identical in both files) and the parent passed it to the operator as its own correction.
   FIX: prompt - a sub-agent's report is a CLAIM, not a measurement; the typed sub-agent result
   header says the same, and the parent must re-check a specific fact or say it did not. WO4 shows
   it working: the parent re-ran the walk itself and reported both numbers when the world moved.
7  a write that matched confirm_patterns reported "OK: wrote N chars" with no sign that a human had
   been asked. FIX: the result says the operator approved it. (The confirm lane itself WORKS:
   the question appears, the answer takes, the write lands - answering it needs `--no-announce`,
   because the announce line's first word is not "yes".)
8  a question about a tool that had never run cost ~20 minutes: the model mined its session files,
   the carry store, the journal, the log, spill/ and its own source, with four re-reads of the
   800 KB build. FIX (prompt): if a result is not in your context, that call did not happen in this
   run - say exactly that, in one line.
   OPEN REFINEMENT: in WO4 the answer DID exist (a real web_search result survived in the box's own
   log), so the clause as written is too broad - it should forbid the file-by-file mining, not the
   one-call look in the log/journal for an EARLIER run. Worth rewording with that evidence.
```

## 3. The toolsmith (the operator's public-release note, now built)

He flagged it mid-session: **the public shapes ship no tools at all**, and the piece he cares about
most is a tool that lets the harness/model know how to make a tool, or at least import one and
index it. That is `tools/toolsmith.py` (native shape, stdlib only, committed b3536b2):

```
new    scaffold a native tool from a short arg spec ('path:str=., top:int=5, human:bool=True')
wrap   import ANY existing script as a manifest tool (command + schema on stdin/stdout)
list   index tools/ by shape, with the NAMES each file registers
check  run the HARNESS's own loader (load_tool_defs - the single reader create_tool and the write
       verifier use) and report its verdict; "OK" is the loader's judgement, not this tool's
```
Packaging lockstep done: `.gitignore` carve-out, `build-package.py` SHIP + APP_FILES,
`build-cli-package.py`'s tools folder; the installers copy `tools/` wholesale so they need nothing.
`tests/test_toolsmith.py` is 28 checks on both builds, and the check caught two real bugs while it
was being built (a JSON `true` written into a Python scaffold - pprint now; a quoted interpreter
path PowerShell mangled into "no verdict" - an argv list now).

## 4. What is staged / waiting

```
DONE-ish, needs the push     section 1 records it: the round-3/4 fixes and the toolsmith are in the
                             tree and on NO host except the manager box's tree (and .20 for round 3).
public tools selection       NOT STARTED beyond the toolsmith. His words: "at the bare minimum a
                             tool creator tool and a selection of useful agnostic ones like
                             computer_use and others you identify". My shortlist to bring him, with
                             reasons and the one decision that is his:
                               ship now   toolsmith (done), screenshot (portable, no shell
                                          equivalent worth writing), http_request (method/body/
                                          headers - fetch_url is GET-only), archive (zip/tar
                                          list+extract; the cross-platform flags are a known pain)
                               his call   computer_use: needs a desktop AND a driver contract we do
                                          not have in this tree; either ship it as a tool that skips
                                          where it cannot run, or say the driver is a separate
                                          decision
release prep (public gate)   still 27 problems, all inside the manager box's own skills/ (fleet-access,
                             mattermost-ops, hermes-ops, openwebui-ops, docker-stack-ops,
                             tech-blog-publishing…); tinycmdr.py / cli / field-notes are clean. The
                             public shape must be cut from a tree without the private runbooks (or
                             they must be pruned). Repro: ~/hermes-tmp/publicgate.py
fleet kit + cleanup          rebuild dist/ at the cut; after the final push, the .bak-push files on
                             each host are litter (his standing rule: an install folder is runtime)
the Windows test box residue            cleaned: my probe files are gone; tmp/wc2-brief.json(+.bak) is the
                             BOT's own delegate brief, left alone. logs/wedge-dump-20260923-1315.txt
                             is the py-spy stack from a run I thought was wedged - keep it as
                             evidence until the next tidy pass
```

## 5. How to work on it (the commands and the kit that were used)

```
regenerate the console build after ANY tinycmdr.py edit:
    python maintenance/build-cli-source.py && python maintenance/build-cli-fix.py
the whole gate:   bash ~/hermes-tmp/sweep-f2f3.sh          # 40 suites + 8 console-build specs
one suite, two builds:
    python tests/<suite>.py ; TINYCMDR_SRC=tinycmdr-cli.py python tests/<suite>.py
falsify a gate:   ~/hermes-tmp/falsify-discovery2.py ; or git show HEAD:<file> > probe-pre.py and
                  TINYCMDR_SRC=probe-pre.py python tests/<suite>.py   (make new checks
                  getattr-tolerant, or a pre-fix build CRASHES the suite instead of failing it)
public gate:      python ~/hermes-tmp/publicgate.py        # stage + redact + audit, no dist write
push:             push-survey.py -> push-arm.py -> push-write.py -> push-verify.py
                  (arm notices first; a host with an active run gets bytes and keeps its process)
fleet line:       powershell -NoProfile -ExecutionPolicy Bypass -File maintenance/fleet-version-report.ps1
drive kit         python bin/mm_say.py --to 6600 --file <order.txt>     # post as the operator
                  python hermes-tmp/tail6600.py <offset>                 # only what grew since
                  python hermes-tmp/r1-grade.py <offset>                 # tool calls + harness notices
                  python hermes-tmp/mm_dump.py 6600 <out.md> [n]         # the answers, newest n
                  python hermes-tmp/probe-load.py <build>                # what a host's python loads
                  python hermes-tmp/prompt-probe.py <build>              # the prompt a host builds
                  python hermes-tmp/route-probe.py                       # the hint, on real commands
                  python hermes-tmp/toolsmith-probe.py                   # the toolsmith, end to end
                  offsets: hermes-tmp/r2-off.txt r3-off.txt r4-off.txt
checking a bot is not wedged (do this BEFORE a restart): py-spy dump
                  C:/Users/<user>\AppData\Local\Programs\Python\Python312\pythonw.exe <pid>
                  via a .cmd over ~/hermes-tmp/wssh.py; a run blocked in the CONFIRM path looks
                  exactly like a wedge (wait on threading.Event) and is not one.
```

## 6. Traps this session paid for

```
- An f-string prompt edit needs DOUBLED braces: `search_files {pattern, path}` inside the
  build_system_prompt f-string evaluated `pattern` and the whole prompt raised NameError. The suite
  caught it; add the field with {{ }} and re-measure the prompt.
- The operator's proxy (me) answering a confirm needs `mm_say --no-announce`: with the announce line
  first, the answer's first word is "[hermes," and the parser reads that as a NO.
- MSYS paths do not work in NATIVE python: open("/c/Users/...") raises, and a poll loop that caught
  the exception looked like "the bot announced nothing" while four hosts had. Use C:/... paths.
- A trap question (about a call that never ran) inside a 3-probe order can eat the whole run: the
  other two probes finished in minutes and the third burned twenty.
- A subprocess with a quoted interpreter path: pass an argv LIST, never one shell string
  (`"C:\...\python.exe" "C:\...\x.py"` came back as "Unexpected token" on PowerShell).
- A Python scaffold needs a PYTHON literal: json.dumps writes `true`/`false`, which is a NameError
  inside a .py file. pprint.pformat for anything embedded in a Python source file.
- The drive's own log line is the ground truth for "did the hint fire": a hint that only rides a
  tool RESULT is invisible to every later reader.
```

## 7. The operator's standing asks, restated

```
- Do not ship without a real problem solved: every fix above cites a measurement.
- He reviews prompt lines: the lines added this session are quoted in section 2 where they matter
  (the route hint, the core/custom wording, the skill clause, the two claim clauses).
- One version per batch; bytes are never urgent; the restart is his call.
- Never restart a host with an active run. Never message a bot about its own lifecycle.
- Public posts: no IPs, no hostnames, no creds; example.com only for a major release.
```
