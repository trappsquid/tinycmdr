# Tinycmdr 1.0.0 — state of the work, 2026-09-20 (evening)

A checkpoint for picking this up in a new conversation. It says where everything is, what is
proven, what is not, and what to do next. No secrets in this file: it names where they live.

## What this project is

Tinycmdr (renamed from `tinycmdr` on 2026-09-20) is a single-file agent harness: one Python file
plus a skills folder, driven by DM from Mattermost, by a local page, or by a session in a
terminal. It runs on six household machines and is about to become public.

```
repo        C:/Users/<user>\tinycmdr          (git, branch main, clean, HEAD = the return-point commit)
return point  git tag 1.0.0-rc-20260920       the validated state; the test plan for the
                                                next pass is docs/test-plan-2026-09-21.md
version     1.0.0                                  (the app and the console build share the number)
download    dist\tinycmdr-1.0.0-win-public.zip      18 files, ~371 KB - the shape a reader gets
github      https://github.com/tinycmdr/tinycmdr     PRIVATE and EMPTY: nothing has been pushed yet
session     Hermes session 20260920_124317_f6782f1a - session_search can recover any detail
```

Nothing has been published. 1.0.0 is the first public number because no public version ever
existed; internal 2.5.x history keeps its old numbers and its old name.

## What is done and verified

**The rename.** `tinycmdr` -> `Tinycmdr` across the tree, the manager box, and four of the five other hosts:
`tinycmdr.py`, `tinycmdr-cli.py`, `tinycmdr-supervise.py`, `INSTALL-WINDOWS.cmd`, the installers, the
scheduled task `Tinycmdr`, `tinycmdr.service`, `com.tinycmdr.agent`, and `TINYCMDR_MM_TOKEN`.
Historical records (CHANGELOG, docs/dev-log.md) deliberately keep the old name.

**The fleet.** Five hosts run 1.0.0. Built-in hash of `tinycmdr.py` on the migrated hosts was
`474f23497ffb0bde`; the manager box's tree is now `971d24a7dedfb378` (the newer defaults + installer work), so
the fleet is one revision behind by choice - pushing it is a separate decision.

**Shipped defaults** (a reader inherits these; the fleet pins its own values, so it is unaffected):

```
llm.max_context_tokens  131072      llm.max_turns 100        llm.request_timeout 1200
agent.tool_carry        true        agent.event_log true     agent.ask_user true
agent.history_exchanges 20          agent.tool_output_max_chars 10000
agent.notes_max_chars   8000        agent.shell_timeout 300
agent.blocked_patterns  21          llm.allow_cloud_fallback false
```

**Packaging.** The download is runtime only: 49 files/550 KB became 19 files/365 KB by dropping
`tests/`, `CHANGELOG.md`, `MANIFEST.txt` and `launch-tinycmdr.sh`. The reference config is checked
against the code by VALUE (not presence), the installer's required-file list is checked against the
package at build time, and the public gate refuses fleet identity, secrets, and the operator's
surname.

**The installer is interactive.** Double-clicked, it asks which doors to set up (any combination of
a local page, a Mattermost bot, sessions by hand), then the model endpoint and model id, then the
page token (generated, or type your own), shows a summary, and waits for a yes. A chat account is
optional: with no token it sets up the local page instead and registers no chat task. An existing
install is a question, not a `-Force` error.

**The web page's token** lives in `.env` as `TINYCMDR_WEB_TOKEN` (an `env_map` override) and
nowhere else: the installer hands over a link that already carries it
(`http://127.0.0.1:8787/?token=...`), clears `web.token` in `config.json`, deletes a stale
`web-token.txt` left by an older install, and prints the ready link on Linux and macOS too (only
Windows did before). The page takes the token from the link, remembers it, and re-asks if the server
says 401, naming `.env`. The link is URL-escaped (`[uri]::EscapeDataString`),
because "type your own password" invites `&`, `#`, `+`, `%` and spaces, each of which means something
else in a query string.

**The page was dead in every browser, in every shape of 1.0.0, until the night of 2026-09-20.** Three
`askToken()` string literals held a SINGLE backslash-n in the Python source, so Python did the escape
and the browser received real newlines inside JS string literals - the whole `<script>` was a
SyntaxError, so no line of it ran: Send did nothing, Enter inserted a newline, the token stayed in the
address bar and the version chip stayed empty. Fixed in `tinycmdr.py`. `tests/test_webui_page.py` graded
the page sliced out of the RAW SOURCE TEXT, which still reads as valid JavaScript, so it was green on a
page no browser could run; it now renders the page the way the server does, its DOM shim covers
`location`/`history`, and it grades the installer's link handover in both directions.
`tests/test_webui_browser.py` is the suite that catches this class (it was failing 13 checks on that
tree, and had not been run since the page changed).

**The page's live reporting, all three gaps closed (night, second pass).** A run now draws its own
line in the transcript where it starts and edits it into `✅ Done - N step(s) in Ts · model X` when
it ends (before this, the Done text lived in the header alone and a reload erased it); the model's
REASONING streams into one dim, tail-capped line (`Destination.shows_reasoning`, page only - chat
keeps it off deliberately); and the heartbeat says `thinking · N chars` / `writing · N chars`
instead of printing a chunk rate as "tok/s" while the answer's char counter sat at 0 through the
whole think. Every one of these is server-side: the page's own JS did not change.

**One install, one `config.json`, one `.env`, three doors (night, third pass).** The console build
used to be a second install: it sat in the download folder with a README telling the reader to create
a `config.json` the installer had just created for them, and it REFUSED to run beside a bot (exit 3
when `tinycmdr.py` and `tinycmdr.lock` were next to it) - the exact folder the installer now puts it in.
`tinycmdr-cli.py` is staged at the package root (no more `cli/`) and installed FLAT beside `tinycmdr.py`
on all three platforms, so a session, the page and the bot resolve the same `config.json`, `.env`,
`notes.md`, `tasks.json`, `sessions/` and `tools/` from `BASE_DIR`. The refusal is gone and a test
asserts the opposite (the console starts in the bot's folder and reads that folder's config). The
wizard and README hand over one console command: `python tinycmdr-cli.py`, the only door that needs
nothing but Python.

**No provider is named anywhere a reader can see.** `DEEPSEEK_API_KEY` was the placeholder in
`.env.example`, in the Windows and macOS installers' key handling, in a Linux installer hint, and in
four code comments. The example is now `MY_PROVIDER_API_KEY` with the rule beside it, the installers
preserve EVERY key a host already had (and say which keys they refused to copy from a shared secrets
file), the macOS installer defaults to the usual local llama.cpp shape, and the comments keep their
lesson with the vendor's name removed.

**The console build is generated in TWO stages.** `maintenance/build-cli-source.py` writes it;
`maintenance/build-cli-fix.py` then applies ~40 edits that refuse unless they match exactly, including
the console-holding helpers. Regenerating with the first stage alone produced a file that imported
cleanly and failed 64 checks in `test_cli`. Run both, in order.

**A page-only install also failed its own readiness check.** The supervisor waited for the lock plus a
Mattermost answer, which an install with no chat account can never produce: it logged
`up but not ready after 90s` in the first two minutes and then booked every later exit as a FAILED
START (`uptime=1488s ready=False (consecutive failures 1)` on the Windows test box), heading for growing backoff
and an "tinycmdr keeps failing" announcement on a healthy bot. It waits for the doors the child was
actually asked to serve now - chat installs keep the old test, a `--web` child is ready when the page
answers - and `tests/test_supervise_ready.py` grades all three lanes against real children.

**Verified on a real install (night, third pass).** Non-interactive install from the rebuilt public
zip into a throwaway folder, 11 checks green: `tinycmdr-cli.py` installed flat beside `tinycmdr.py`;
`.env` carries `TINYCMDR_WEB_TOKEN`, `config.json` holds no token, no `web-token.txt`; the console
build reads THAT `config.json` (the endpoint it reported came from that file) and THAT `.env`
(`TINYCMDR_MODEL` from the .env reached the run); the page answers 401 with no token, 401 with a wrong
one, 404 with the `.env` token. the Windows test box's live install was migrated in place (its existing token
moved into `.env` unchanged, so the link he already has still works; `config.json` cleared;
`web-token.txt` deleted; restarted; `python tinycmdr-cli.py --version` in `C:\tinycmdr` returns rc=0
where the old build refused).

**Verified today on the Windows test box** (each of these was run, not assumed): the installer's wizard in
every lane (page, chat, page+chat, sessions-only), a token-less local install serving the page
(`{"ok": true, "version": "1.0.0"}`), the page's auth (`401` without the token, `401` with a wrong
one, `200` with the right one), a redo keeping a state file it found, and the public archive
unpacking to a clean tree. All 22 suites pass, `test_cli` at 168 checks.

That list was all SERVER-level, and it is how a page no browser could run passed every gate: `/api/health`
answering `ok` and the 401/200 checks say nothing about the page's own JavaScript. The page is now graded
in a real engine twice over - `test_webui_browser.py` (Edge through Playwright, typing and pressing Enter
on the composer) and a headless Edge run against an install made from the built
`tinycmdr-1.0.0-win-public.zip` (version chip `1.0.0`, the message sent, `web run ... started from the
browser`, no JS exceptions).

## Open, in the order they should happen

1. **His re-test of the doors** on the Windows test box - the install he made is kept, not redone. It now also
   carries the console build: `C:\tinycmdr\tinycmdr-cli.py` (`4112f5ab1c2cba71`) is the thing to open for
   a session in that folder, and `tinycmdr.py` (`d81b040cddd2c8b1`) has the page token moved into `.env`.
   `tinycmdr-1.0.0-win-public.zip` (`3076caa0039fa1b6`) and its `.sha256` sit in that host's `Downloads`
   with the extracted folder refreshed, so a clean re-run of the wizard is available too.
   `Z:\VPS Admin` carries all five rebuilt artifacts with fresh hashes.
2. **The GitHub push**: a LICENSE (MIT, `Copyright (c) 2026 David Trapp`), the first commit, then
   flip the repo to public after his review. Three things to handle first:
   - the public gate refuses `\bDavid\b`, so the LICENSE needs an explicit exception;
   - commit authorship uses the masked GitHub address, so history is public-safe from the start;
   - **THE REPOSITORY HAS NOT BEEN AUDITED FOR THE FLIP.** The packages are gated, the *repository*
     is not: `docs/` and `CHANGELOG.md` name hosts, folder paths and incidents, and git history
     keeps every one of them. Flipping this repo public as-is would publish all of that. Either keep
     the dev repo private and publish from the gated package, or strip/gate `docs/` first. This
     document names hosts, so it belongs in the private tree only.
3. **the LAN model box** is the one host still on the old name and 2.5.23 (`~/tinycmdr`, `tinycmdr.service`).
   It migrates in a quiet moment: it also serves the main inference box.
4. **Pushing the newer build to the five hosts** when he says so (defaults + installer changes).
   The only behavioral flip: the MacBook sets no `ask_user`, so that turns on with the new bytes.
5. **Small leftovers**: the empty `C:\tinycmdr` folder on the Windows test box (held by a stale handle, zero
   items - still there tonight); the macOS label on the MacBook still reads `com.trapp.tinycmdr` (its
   own install, so it keeps working - rename it when that host is next touched); on the Windows test box
   `tinycmdr.py.bak-pageescape-20260920` is the version that had the dead page, safe to delete once he
   has re-tested.
6. **A judgment call for him**: the internal fleet kit still carries `skills/.imported-unused/`
   (570 files, 7 MB of packs literally named unused). Offer stands to exclude it.

## How to work on it

```
build every shape      python maintenance/build-package.py [--public] [--public --macos]
                       python maintenance/build-cli-package.py
the console build      maintenance/build-cli-source.py then build-cli-fix.py - it is GENERATED,
                       never hand-edited
suites                 python tests/test_*.py            (22 suites, all green)
fleet build report     powershell -File maintenance/fleet-version-report.ps1   (read-only)
hosts and doors        skill 'fleet-access'; wssh.py for Windows hosts; \\host\C$ over SMB
build/release rules    skill 'tinycmdr-build-release' (the packaging and installer lessons)
```

Traps that cost time today, so they do not cost it again:

- **A probe install must pass its own `-TaskName`** (e.g. `TinycmdrProbe`) and unregister it. A
  probe that registers `Tinycmdr` overwrites the operator's task, and a cleanup that only deletes
  folders leaves a task pointing at nothing.
- **`-VerifyOnly` never reaches the installer's package-integrity check.** Only a real install into
  a temp `-InstallDir` proves a package.
- **The `.cmd` wrapper always passes `-NoPause`**, so never gate questions on that flag: a real
  console decides whether to ask (`-NonInteractive` and a redirected stdin mean "do not ask").
- **Dropping a shipped file means editing four places** in lockstep (`SHIP`, `APP_FILES`, the
  installers' copy lists, and the installers' required list) - the required list is the one that
  breaks the install loudly.
- **Renaming a shipped file means grepping the packager**, not just the tree.

## Where the credentials live (not in this file)

```
per-host bot tokens      Desktop\Mattermost-BOT-TOKENS.txt (the manager box)
per-host model keys      Desktop\Tinycmdr-DEEPSEEK-KEYS.txt (the manager box) - a model key is per bot
the Windows test box's own          that host's Downloads\the Windows test box-secrets.env; backup of its pre-test
                         state under hermes-tmp\release\the Windows test box-preinstall\ on the manager box
web UI tokens            .env (TINYCMDR_WEB_TOKEN) in each install
NAS credentials          %USERPROFILE%\.nas-cred (used by bin\nas-connect.cmd on the manager box)
```
