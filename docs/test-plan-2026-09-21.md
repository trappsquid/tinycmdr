# tinycmdr 1.0.0 — the Windows-install validation pass, for 2026-09-21

Written at the end of 2026-09-20 (night). Point tomorrow's conversation at this file plus
`ATTAGOS-1.0.0-STATE-2026-09-20.md`: this one is the test plan, that one is the state of the work.
No secrets here; it names where they live.

## The return point

```
git tag          1.0.0-rc-20260920 (annotated, repo = C:/Users/<user>\tinycmdr)
                 commit 3e53613 = "1.0.0: one install, one config.json, one .env - the doors are mediums"
restore the tree git checkout 1.0.0-rc-20260920           (nothing is pushed anywhere)
artifacts        Z:\VPS Admin\ — five files with .sha256 beside each:
                 tinycmdr-1.0.0-win-public.zip       5d83f60513d14faed39023227326b70890402911bd4b845e846945a653cc900c
                 tinycmdr-1.0.0-linux-public.tar.gz  ae1a9e6709d359355ef5a613aab67e9f334fd1ef90023520da68d4fca7b2b1b0
                 tinycmdr-1.0.0-macos-public.zip     63c2274a70b558373d080f296c4714ce5fa7a3b0a88efabf84ebd30523f156c9
                 tinycmdr-cli-1.0.0-win-public.zip   db32c17fd5f965c1e93a80abcf4d89ecb32ae0086b3fad426437f08bc8c0b454
                 tinycmdr-cli-1.0.0-linux-public.tar.gz ecda2b087eaef1222c8ceb2c73ca5dc78abd86248343783bea7903ab1aa087ec
rebuilt 2026-09-21  a.m.  the endpoint-window fix and the reworded no-answer warnings are
                 in these bytes (docs/dev-log.md); the old five hashes are superseded.
the Windows test box now    CLEAN for the walk: task "tinycmdr" unregistered, no pythonw, C:\tinycmdr removed.
                 State before the wipe: the manager box hermes-tmp\release\tinycmdr-the Windows test box-preclean-20260921-0707
                 (32 files incl. .env — the manager box only). Downloads has the rebuilt zip + its .sha256.
a copy of it     the manager box hermes-tmp\release\tinycmdr-postunify-20260920\ (23 files, incl. .env — the manager box only)
older state      the manager box hermes-tmp\release\tinycmdr-pre-wipe-20260920-185735\ (the install before tonight)
```

## What changed since the last install he walked (so he knows where to look)

```
the console      tinycmdr-cli.py is now INSTALLED, flat beside tinycmdr.py in C:\tinycmdr, by all
                 three installers (it used to sit in the download folder asking for a config.json
                 the installer had just created). One command in the wizard and the README:
                 python tinycmdr-cli.py
the refusal      it used to exit 3 in a folder that held a bot ("copy this file into a folder of
                 its own"). That folder IS the install now, so the refusal is gone: a session, the
                 page and the bot share config.json, .env, notes.md, tasks.json, sessions/, tools/
the page token   .env (tinycmdr_WEB_TOKEN) instead of config.json + a loose web-token.txt; the 401
                 prompt names .env; Linux and macOS get the ready link too (only Windows did)
provider names   none anywhere a reader can see: .env.example, both installers' key handling, a
                 Linux installer hint, four code comments, the macOS default endpoint
unchanged        the page's own JavaScript (the streaming/Done-line/reasoning work is server-side)
```

## Already proven tonight — do not spend tomorrow re-doing it

```
suites           22 suites, 0 failures; test_cli 167 checks against the regenerated build
CLI packages     rebuilt; their gate re-runs test_cli on a clean unpack (167/0)
a real install   from the built win-public zip, non-interactive into a throwaway dir, 11 checks:
                 - tinycmdr-cli.py installed flat beside tinycmdr.py
                 - .env carries tinycmdr_WEB_TOKEN, config.json holds no token, no web-token.txt
                 - the console build read THAT config.json and THAT .env (both proved by values
                   from those files appearing in its own output)
                 - the page: 401 with no token, 401 with a wrong token, 404 with the .env token
the Windows test box        migrated in place, restarted, /api/health ok, page token still the same one he
                 already had, console build returns rc=0 in C:\tinycmdr where the old build refused
```

## The walk-through for tomorrow (his own reader path, on a clean machine state)

```
0. clean start   the Windows test box: unregister task "tinycmdr", kill pythonw, delete C:\tinycmdr
                 (its current state is copied to the manager box hermes-tmp\release\tinycmdr-postunify-20260920)
                 or use any spare box — nothing here is host-specific
1. download      check the sha256 of tinycmdr-1.0.0-win-public.zip first, then extract
2. install       double-click INSTALL-WINDOWS.cmd and walk the wizard. Watch for:
                 - the door question offering the three mediums
                 - the console command it prints being "python tinycmdr-cli.py"
                 - the ready link carrying the token, URL-escaped
3. console door  cd C:\tinycmdr ; python tinycmdr-cli.py — a session should start, and /status
                 should report the same endpoint the bot uses
4. page door     follow the printed ready link: page loads, token stripped from the address bar,
                 version chip 1.0.0, a message streams, the run draws its own line and edits it
                 into the Done line, reasoning (if the model thinks) streams dim, Stop works
5. both at once  with the task running, start a console session in the same folder: it must NOT
                 refuse, and the bot must keep answering
6. a redo        install-tinycmdr.cmd -Force over the live install: keys in .env survive, one
                 config.json still, no web-token.txt appears
7. the folder    look at C:\tinycmdr as a reader: anything that reads as litter (see below)
8. uninstall     the documented removal path
```

## Open decisions for tomorrow (mine to raise, his to settle)

```
1. the examples   config.example.json and .env.example are still COPIED into the install dir, so a
                  fresh install has the template sitting next to the live file. He asked about
                  this today and there is no decision yet. The candidate edit: let the installer
                  read the templates from the package and stop copying them into the install
                  (the reference stays in the download, where a reader browses).
2. 401 wording    the page's prompt now says ".env (tinycmdr_WEB_TOKEN)" — worth a reader's eye
3. the Windows test box      tinycmdr.py.bak-pageescape-20260920 and tinycmdr.py.bak-1939-prestream are safe to
                  delete once he has re-tested; C:\tinycmdr (empty, stale handle) is still there
4. the GitHub flip  THE REPOSITORY HAS NOT BEEN AUDITED for public: docs/ and CHANGELOG.md name
                  hosts, paths and incidents, and git history keeps every one of them
```

## Resume commands

```
build every shape    python maintenance/build-package.py [--public] [--public --macos]
                     python maintenance/build-cli-package.py
the console build    maintenance/build-cli-source.py THEN maintenance/build-cli-fix.py — both, in
                     that order. The first stage alone produces a file that imports cleanly and
                     fails 64 checks (the fix stage inserts the console-holding block).
suites               python tests/test_*.py        (or hermes-tmp\run-suites.sh for all 22 at once)
real-install probe   python hermes-tmp\wininstall-unified-probe.py   (installs from dist\ into a
                     throwaway folder and grades the 11 checks above)
deploy to the Windows test box  copy tinycmdr.py + tinycmdr-cli.py to \\a LAN address\C$\tinycmdr, then
                     powershell -File C:\tinycmdr\maintenance\restart-tinycmdr.ps1 (via wssh.py)
hosts and doors      skill 'fleet-access'; hermes-tmp\wssh.py for Windows hosts; \\host\C$ over SMB
build/release rules  skill 'tinycmdr-build-release'
```

Traps that cost time today, so they do not cost it again:

- **The `.ps1` and `\r`**: the patch tool turned `maintenance\restart-tinycmdr.ps1` inside a
  PowerShell string into a real newline. Edit escape-heavy files with a Python script using raw
  strings, then parse-check the result (`[scriptblock]::Create`).
- **Line endings per file**: `tinycmdr.py`, the installers and the docs are not all the same
  (CRLF vs LF). A byte-level edit must convert its anchor to match the file it is editing.
- **`test_cli` grades the generated build**, so regenerate it BEFORE running the suite, or the
  suite grades yesterday's file.
- **A probe install must pass its own `-TaskName`** and unregister it; `-VerifyOnly` never reaches
  the installer's package-integrity check.
- **`/api/health` is deliberately open** (the supervisor's readiness gate polls it), so the token
  is graded on a protected endpoint: `/api/events` answers 401 without it, 404 with it.
