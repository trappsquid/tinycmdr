# Tinycmdr 1.0.0 — state of the work, 2026-09-23 (tool visibility + the first operator drive)

A checkpoint for picking this up in a new conversation: what landed, what is deployed, what the
drive found, what is waiting on the operator's word, and how to repeat any of it. No secrets here:
it says where they live.

```
repo        C:/Users/<user>\tinycmdr (the manager box; the repo IS the install), branch main
HEAD        59a1ede  fold the host-local comment fix from the Linux test box
            0932953  discovery surface line: trim blurbs at a word boundary
            ea36983  tool visibility: discovery stops answering with a wrong tool, misses name
                     the whole surface, delegate_task yields a typed result
            b41c421  prompt batch (Opus 5.5 leak audit): shell routing, confidence, verify
VERSION     1.0.0  UNPUBLISHED. No bump: a bump is owed at the release, not per edit.
deployed    five hosts, all three files, sha256 tinycmdr.py 472b6bc9313170c2 (see below)
```

## 1. What landed in the tree today

Three batches, all committed, all gated. Suites were not touched except where noted.

```
b41c421  prompt batch from the leaked Opus 5.5 prompt (elder-plinius/CL4R1T4S, extract at
         ~/hermes-tmp/opus55/, audit at ~/hermes-tmp/tinycmdr-opus55-prompt-audit.md):
         N1 file work goes through the harness tools, not the shell (read_file, search_files,
            edit_file) — measured basis: 67 of 102 shell calls on the Windows test box were file-grep/list
            work and search_files was called ZERO times;
         N2 soul.md: "Being sure from memory is not evidence: look it up however familiar it
            feels" — the one gap left in the cutoff rule;
         N3 checking the work is the last ledger item, and high-stakes work gets a fresh-eyes
            delegate_task verifier.
         static prompt 17,584 -> 18,081 chars.
ea36983  tool visibility, the operator's "make the harness and model see and use its tools" batch:
         R1  _match_tools scores only DISCRIMINATING words (_TOOL_STOPWORDS, _squash for
             subagent/sub-agent). Measured before: "send Mattermost message to channel"
             revealed `schedule`, "…post message channel thread" revealed `blog`, "…to agent
             channel via API" revealed `delegate_task`, each headed "now callable", each wrong;
             35 minutes of a run then went into rebuilding a capability the box did not have.
         R2  a miss now names this box's whole remaining surface (_surface_tail, bounded);
             the empty-query form is the deliberate "show me everything" call; list_tools stays
             ONE line (pinned <220 chars in test_stall) and forwards to find_tools.
         R3  one prompt line: a single find_tools call is the check, a missing tool is a thing to
             SAY. The credential half of the proposal was DROPPED: the operator is unconcerned
             about bot tokens / bot admin rights (2026-09-23).
         R4  delegate_task yields a TYPED result: the harness appends _SUBAGENT_RESULT_CONTRACT
             to the sub-agent's task, parse_subagent_result validates the fenced ```result block
             (status ok|blocked|failed, summary, evidence, blockers, followups), the parent gets
             fields plus the sub-agent's own words. FAIL-SOFT: no block / bad JSON / a JSON array
             / an invented status / an empty summary all come back UNPARSED with the prose.
         new gates: tests/test_tool_discovery.py (40 checks), tests/test_subagent_result.py (34),
         both green on BOTH builds. Falsified first: HEAD's _match_tools swapped back in makes
         test_tool_discovery fail 11 checks, starting with the three measured queries.
0932953  surface blurbs trim at a word boundary (the first cut read "…then call it immediatel").
59a1ede  folded the Linux test box's HOST-LOCAL comment fix into the canonical tree: its cli said "a bot
         account died at its 32 GiB cgroup" where ours said "a bot account died…", and PUBLIC_FORBIDDEN
         bans \ba bot account\b, so the public build refused the whole tree. A host-local fix is a fork
         that the next push reverts — fold it in.
```

Costs after all three: static prompt **18,316 chars**; always-on tool schemas **8,518 of the
8,900** gate (unchanged — every addition is prompt text or an outcome the model already pays for).

## 2. What is deployed (push, 2026-09-23 ~11:35-11:40 local)

```
the manager box        local          restarted through its own page (/restart, web.token in config.json)
the other Windows box .9  supervisor     child killed, relaunched; connects as @the other Windows boxnew (account name has
the Windows test box .20 supervisor    changed: it is @6600 now, not @wintest) — read the account off the
                            host's own log, never off a list
the Linux test box .13 systemctl    sudo -n systemctl restart tinycmdr; connects as @the Linux box
the LAN model box .47   systemd        BYTES ONLY, keeps its process (the model box; standing rule)
MacBook .3  launchd        ASLEEP / unreachable: still carries the previous build
all five: tinycmdr.py 472b6bc9313170c2 · tinycmdr-cli.py 6b919f84f0a7998c ·
          tinycmdr-supervise.py 200ead14c7909da3, hashes read back ON each host
notices:  every restart announced itself ("posted startup notice to <its channel> (downtime Ns)");
          the arm keys were popped, i.e. the notice path ran
fleet-version-report.ps1: 1.0.0, five hosts in sync, watchdog column supervisor|systemd
```

Push mechanics, reusable: `~/hermes-tmp/push-survey.py` (read-only survey: hashes, config,
in-flight evidence, the BARE notice channel from the host's own log), `push-arm.py`, `push-write.py`
(byte copies + `.bak-push<stamp>`, hash read back on the host), `push-verify.py` (supervisor status
+ the two log lines that matter + /api/health). Windows files go over SMB, Unix over the ssh
aliases; restarts only through each host's own door.

## 3. The first operator drive (and what it found)

Method: I posted as the operator (the standing door — `python ~/bin/mm_say.py --to 6600
--file <msg>`, which authors the message as @david and announces the real sender in line one) into
@6600's DM, then graded the answers against the tree. The bot's log and its session transcript are
readable over SMB: `\\a LAN address\C$\tinycmdr\tinycmdr.log` and
`sessions\mm-<id>.json`. Work orders are in `~/hermes-tmp/wo1..3.txt`.

Verified working in the wild:

```
find_tools({"query": "delegate a subtask to a sub-agent"}) -> 1247 chars, revealed delegate_task,
  model used it. R1/R2 earn their keep when the model actually asks.
delegate_task -> the parent reported the yield as "structured fields (JSON-like: storage path,
  write_sites table, helpers, blockers, followups) with a short prose block appended", and the
  sub-agent's line numbers were EXACT against the tree: TASKS_FILE 77, atomic_write_text 3880,
  journal_tasks 3972, save_tasks 4012, call sites 4099/4131/4139. R4 verified end-to-end.
the ledger was used properly (add -> done with the evidence in the note).
```

Findings (all still open; none of them fixed yet):

```
F1  the fleet's installs have an EMPTY tools/ — no `patch`, no `process`. Cause, measured:
    dist/tinycmdr-1.0.0-win.zip was built 09-22 19:58 and tools/patch.py was written 23:29 that
    night (commit 4f176af, "two starters"). The FLEET KIT predates the starter tools (the public
    shapes have them: 3-4 tools/ entries), so every host installed from the kit has none, and
    list_tools answers "custom = none". The installers are NOT at fault (both copy tools/).
    FIX: push tools/patch.py + tools/process.py + tools/README.md to the five hosts (additive,
    hot-loaded, nothing to clobber) and rebuild the fleet kit at the next cut.
F2  the model will not spend the discovery call. It answered "which tool edits by a fuzzy anchor"
    with edit_file (exact-match), enumerated 3 of the 9 hidden tools, and — after being told
    "get it from a tool call" — still only called list_tools. Across three runs find_tools was
    called ONCE, and only when the query named a capability. It reaches for list_tools because
    the NAME matches its intent; that reply is one line by design.
    FIX (recommended, cheap, agnostic): a STATIC line in build_system_prompt naming what exists
    but is not in the list, generated from the build so it cannot drift, e.g.
      "Also on this box, not in your tool list — call one by name and it stays for the session:
       <hidden core tools by name>. Plus any custom tool this box has installed."
    ~200 chars, static => cached prefix => one prefill per session (~0.2s), zero per turn.
    Reason to prefer this over widening list_tools: the failure is that no call is made at all.
    If list_tools IS widened, tests/test_stall.py's <220-char pin must be updated WITH a reason
    (that pin was measured against repeating the always-visible core list, not against naming
    hidden tools).
F3  a "verification" that does not test the claim: it proved write_file wrote a file by reading
    that the file exists. ~120-char clause on the existing verify bullet fixes the class
    ("the check must test the claim itself — a file existing does not prove what wrote it").
F4  minor: its first probe answer was simply wrong about the fuzzy-anchor tool — but with F1 true
    that box has no such tool, so "none here" was the honest answer. This is why F2 matters.
```

## 4. Waiting on the operator's word — ANSWERED; the work landed (detail in section 7)

```
STATUS 2026-09-23 (later session): F1's files are pushed and hash-verified on every reachable
      host, F2 + F3 are in the tree as 69f66d0 with 16 new checks and a green sweep. What is
      still owed is the RESTART wave (his call, and the only thing that makes either live).
F1  push the three starter tools (a fleet change) + rebuild the kit at the cut
F2  the static hidden-tools inventory line + its test (a prompt change: he reviews prompt lines)
F3  the verification clause (fold in with F2 if approved)
then keep driving: probes aimed at shell-vs-harness routing (N1) and the completion-spin path
    (completion announced N times while tool calls queue) — neither has been exercised yet.
```

## 5. Open, unrelated to today's batches

```
- PUBLIC BUILD GATE: auditing the public stage from THIS tree reports 27 private-string problems,
  all inside the manager box's own skills/ (fleet-access, mattermost-ops, hermes-ops, openwebui-ops,
  docker-stack-ops, tech-blog-publishing…), while tinycmdr.py / tinycmdr-cli.py / field-notes.md
  are clean. That is a release-prep matter: cut the public shape from a tree without the private
  runbooks (or prune them), not a defect in the app. Reproduce with ~/hermes-tmp/publicgate.py.
- The fleet kit in dist/ is stale (predates the starter tools, see F1); a rebuild happens at the
  next real cut. Do NOT rebuild dist/ under 1.0.0 casually: a published/validated artifact is the
  canonical bytes for its version.
- tests/ has 38 suites; the sweep script is ~/hermes-tmp/sweep-n1n3.sh (rename its LOG var per
  batch). It runs every suite plus the console build's own view of the shared ones.
```

## 6. How to work on it (the commands that were used)

```
regenerate the console build after ANY tinycmdr.py edit:
  python maintenance/build-cli-source.py && python maintenance/build-cli-fix.py
  (the pair is reproducible: running it twice leaves the sha unchanged)
the whole gate:   bash ~/hermes-tmp/sweep-n1n3.sh          # 36 suites + 8 console-build specs
one build:        TINYCMDR_SRC=tinycmdr-cli.py python tests/<suite>.py
new gates:        python tests/test_tool_discovery.py ; python tests/test_subagent_result.py
falsify a gate:   ~/hermes-tmp/falsify-discovery2.py       # swaps HEAD's scoring back in
public gate:      python ~/hermes-tmp/publicgate.py        # stage + redact + audit, no dist write
push:             ~/hermes-tmp/push-survey.py -> push-arm.py -> push-write.py -> push-verify.py
fleet line:       powershell -NoProfile -ExecutionPolicy Bypass -File maintenance/fleet-version-report.ps1
drive:            python ~/bin/mm_say.py --to 6600 --file <wo.txt>
```

Traps this session paid for (also written into the skill):

```
- a host-local fix is a FORK: .13's one-word comment change was the difference between a public
  build and a refusal, and the push would have reverted it. Diff a host's file against the tree
  before pushing, not after.
- the fleet kit can PREDATE an addition to SHIP: compare dist/<archive> mtime against the file's
  mtime before blaming an installer.
- read a bot's ACCOUNT off its own log (the other Windows box is @the other Windows boxnew now, the Windows test box is @6600) — a list of
  account names goes stale silently.
- polling a bot's log from the START re-reads megabytes: seek to the current size and read only
  what grows. A poll that shows nothing for 200s can simply be a long model call, not a wedge.
```

## 7. F1/F2/F3: what the second session did (2026-09-23, later)

```
commit 69f66d0  hidden-tool inventory line in the static prompt, and a check must test the claim
        tinycmdr.py + the regenerated tinycmdr-cli.py + tests/test_tool_discovery.py
        + tools/README.md

F2  hidden_inventory_line(), beside hidden_tools(): build_system_prompt carries ONE line,
    generated from hidden_tools(None) so a new hidden tool cannot fall out of it, and gone when
    tool_disclosure is false (every tool is in the payload then, so the line would lie). Prompt
    text, verbatim (static => cached prefix => one prefill per session, nothing per turn):
      "- Also on this box, not in your tool list - call one by name and it stays for the
       session: create_tool, delegate_task, notes, plan, schedule, search_files, search_sessions."
    plus, on a box with custom tools: " The custom tools further down work the same way."
    It sits directly under the find_tools bullet. Static prompt 18,316 -> 18,649 chars.
F3  the check bullet now reads: "... open the page, and make the check test the claim itself -
    a file existing proves nothing about what is in it or who wrote it. For anything high-stakes,
    hand the check to delegate_task so the work is not grading itself."
    (the paragraphs here use a plain hyphen where the prompt uses an em dash)
gates   tests/test_tool_discovery.py 40 -> 56 checks: generated-not-typed, the config-drift case
    (core_tools hides one), the disclosure-off case, the bounded size, and the two F3 clauses.
    Falsified: against HEAD's tinycmdr.py 12 fail / 44 pass. Sweep green: ~/hermes-tmp/
    sweep-f2f3.sh -> sweep-f2f3.log, 38 suites + 8 console-build specs, SWEEP_FAIL=0. The CLI
    build regenerated clean (no anchor refusals); the one check it drops is the scheduler one,
    by design.
    publicgate.py unchanged: 27 problems, all in the manager box's own skills/ (release-prep matter), none
    in the files this batch touched.

F1  pushed tools/patch.py + tools/process.py + tools/README.md, sha256 read back ON each host:
      .9  the other Windows box     all three    fa357fa3 / 7a37437c / 954863f5
      .20 the Windows test box   all three    same hashes
      .47 the LAN model box       all three    same hashes
      .13 the Linux test box   ALREADY carried the two .py, byte-identical to the tree (so section 3's
                      "every install has an EMPTY tools/" was true of .9/.20/.47, not .13);
                      README.md refreshed
      .3  MacBook     asleep / unreachable - still needs all three when it wakes
    Load probe (that host's own python, importing the app and reading REGISTRY.custom): patch and
    process load on .9, .20, .13 and .47.
    NOT LIVE YET, and section 3 was wrong about it: a file dropped into tools/ is read at the
    next PROCESS START, not the next message. ToolRegistry.load_all() runs in __init__ and
    nothing rescans; only create_tool hot-loads (via reload_tool). Measured: a file written into
    tools/ after import is not in REGISTRY.get() and not in the payload. tools/README.md said
    "live on the next message, no restart" - corrected in this commit to say the next start.
    Process starts, for the ordering: .9 and .20 children 11:36:46 local today, .47 05:39 UTC,
    .13 18:36 UTC (after its tools landed, so .13 is the one host already running them).
    Still owed: the fleet-kit rebuild at the next cut (never rebuild dist/ under 1.0.0).
```
