{
  "_readme": [
    "config.json for tinycmdr. One example, every key, fake values.",
    "",
    "The installer copies this file to config.json and fills in the four values",
    "below from its own flags, or from the answers you give it. If you are writing",
    "one by hand, copy this and replace the placeholder values. Delete any key you",
    "do not care about and the code's default applies; the values shown ARE those",
    "defaults except where a _comment says otherwise.",
    "",
    "FOUR values are mandatory or the bot will not start:",
    "  mattermost.url            your Mattermost host, no scheme (chat.example.com)",
    "  mattermost.token          a bot account token. Leave it empty here and put",
    "                            tinycmdr_MM_TOKEN in .env instead: .env is the one",
    "                            place the agent cannot read into a prompt.",
    "  mattermost.allowed_users  your Mattermost user id, or the bot is deaf",
    "                            (it is deny-by-default, on purpose)",
    "  llm.base_url / llm.model  any OpenAI-compatible endpoint and the model id",
    "                            it advertises (llama.cpp, vLLM, Ollama, a cloud",
    "                            provider)",
    "",
    "Addresses here are deliberately unroutable documentation ranges",
    "(192.0.2.0/24, example.com), so copying this file cannot accidentally point",
    "at somebody's real service. A token does NOT belong in this file.",
    ""
  ],
  "llm": {
    "_comment": "Which endpoint answers, and how hard it is allowed to work. base_url is any OpenAI-compatible /v1 root; a local llama.cpp, vLLM or Ollama box, or a cloud provider. Sampling (temperature, top_p, top_k) is deliberately NOT configurable: it is inherited from whatever endpoint you point at, so switching models or providers cannot leave a stale setting behind.",
    "base_url": "http://192.0.2.10:8081/v1",
    "api_key": "none",
    "model": "my-model-name",
    "max_turns": 100,
    "max_context_tokens": 131072,
    "max_tokens": 16384,
    "final_max_tokens": 8192,
    "max_tokens_ceiling": 65536,
    "request_timeout": 1200,
    "request_grace": 30,
    "retry_after_max": 60,
    "no_think": false,
    "fallbacks": [
      {
        "_comment": "Optional. Tried in order when the primary fails. alias is what /model <alias> switches to. Prefer api_key_env over api_key so the secret stays in .env.",
        "base_url": "https://api.example.com/v1",
        "model": "some-cloud-model",
        "api_key": "",
        "api_key_env": "MY_PROVIDER_API_KEY",
        "alias": "cloud"
      }
    ],
    "allow_cloud_fallback": false,
    "stream": true,
    "stream_idle_seconds": 120,
    "_stream": "Model calls use SSE when true: the first token is visible while it generates, a cancel closes the socket (a local llama.cpp stops generating), and a wedged stream ends on stream_idle_seconds. false = one blocking POST per call, which is what every version before 1.9.28 did.",
    "profiles": {
      "example-cloud-model": {
        "tool_output_max_chars": 40000,
        "fetch_max_chars": 60000,
        "notes_max_note_chars": 4000
      }
    }
  },
  "telegram": {
    "_comment": "The third door: a Telegram DM. OFF unless you set tinycmdr_TG_TOKEN in .env - the token is ENV-ONLY, so one pasted at 'token' below is ignored (and logged as ignored), because .env is the one place the agent cannot read into a prompt. allowed_users takes NUMERIC Telegram ids (message @userinfobot for yours) and is deny-by-default: an empty list starts the process, refuses, and says the id is missing. Mattermost WINS when both doors have a token, so this lane never starts on a Mattermost bot - run `python tinycmdr.py --telegram` for a Telegram-only process. DM only: a group message is refused. It shares the same sessions, notes, tasks and skills as every other door.",
    "token": "",
    "allowed_users": []
  },
  "mattermost": {
    "_comment": "Where the bot lives. url is the host only, scheme and port are separate keys. token: leave empty and set tinycmdr_MM_TOKEN in .env instead - .env is the one place the agent cannot read into a prompt. allowed_users are Mattermost USER IDS, not usernames (Profile -> copy the id), and the list is deny-by-default. ssl_verify false is for a self-signed certificate on your own LAN.",
    "url": "chat.example.com",
    "scheme": "https",
    "port": 443,
    "token": "",
    "allowed_users": [
      "REPLACE_WITH_YOUR_MATTERMOST_USER_ID"
    ],
    "ssl_verify": true
  },
  "search": {
    "_comment": "Optional web search for the web_search tool. AnySearch first, Tavily as the fallback provider. Keys normally live in .env. With no keys the tool still tries an anonymous tier.",
    "anysearch_api_key": "",
    "tavily_api_key": "",
    "max_results": 5
  },
  "web": {
    "_comment": "Off by default: the chat build is driven from Mattermost and does not open a port unless asked. `python tinycmdr.py --web` turns it on for that process and serves the browser view - the agent's live activity as it works, not just the final answer - plus /api/chat and /api/health. Set enabled true to serve it while the chat bot runs too. An empty token means loopback only; set one to reach it from another machine, and firewall that port. The installer generates a token and writes it to .env as tinycmdr_WEB_TOKEN, which takes precedence over this key - one secrets file per install.",
    "enabled": false,
    "port": 8787,
    "token": "",
    "host": ""
  },
  "agent": {
    "_comment": "Behaviour: names, budgets, progress reporting, guard rails. The defaults are conservative for a small local model; the numbers below are what the code ships unless a _comment says otherwise.",
    "bot_name": "tinycmdr",
    "history_exchanges": 20,
    "_comment_budgets": "Per-task ceilings. max_steps counts tool calls, max_minutes is wall clock. When the clock runs out the model is forced to summarise what it has, so a run always ends with an answer instead of silence.",
    "max_steps": 250,
    "max_minutes": 75,
    "shell_timeout": 300,
    "_comment_shell": "The Windows interpreter, and a CONSOLE-BUILD setting: the cut-down build reads this and can run cmd (/c) or PowerShell, for hosts where PowerShell is restricted or removed. The supervised build ignores it and always runs PowerShell on Windows, bash -c on Linux and macOS.",
    "shell": "",
    "_comment_scan_bound": "The ceilings on a broad scan (item 6). A command that walks a whole root is classified as a scan: it gets search_timeout seconds instead of shell_timeout, and the seconds it actually spends are charged against one per-run budget shared by shell and execute_code. Both are on by default, because the measured failure was a run spending minutes walking a root and someone waiting; command_cost_guard false lifts the whole thing and scan_budget_seconds 0 lifts only the per-run half. Measured on the graded set: the worst command fell from 608 s to 60 s, and the run budget did not fire in a two-sample probe.",
    "search_timeout": 60,
    "command_cost_guard": true,
    "scan_budget_seconds": 120,
    "tool_output_max_chars": 10000,
    "_comment_result_shaping": "What the agent is handed after a tool runs. digestion renders a known command shape (journalctl, systemctl status, apt/dnf/pip, docker ps, grep, process lists, *.log files) down to its signal, says what it dropped, and any call may pass raw=true for the full text. field notes append the known cause to a FAILED result whose signature is understood, read from field_notes_file: keep that library small, and only for signatures that essentially always mean that cause, because a note that guesses wrong costs steps. verify_after_write parses a file the agent just wrote (python, json, toml, yaml, bash, plus the custom-tool contract) and puts the verdict in the same result; it costs no prompt tokens.",
    "digest_enabled": true,
    "digest_min_chars": 1200,
    "digest_lines": 40,
    "field_notes_enabled": true,
    "field_notes_file": "field-notes.md",
    "field_notes_max": 2,
    "verify_after_write": true,
    "verify_max_bytes": 2000000,
    "_comment_tool_carry": "Carried tool results (ON by default since 1.0.0). A session's earlier tool results can ride into the next run of the same session, bounded per result and in total, newest first, deduped by tool and arguments, age-stamped and marked when a file has changed since it was read. Measured on the fleet manager: the later runs of a session re-bought 80% and 78% less source text, on four measured runs per leg. It ships OFF, because the failure mode that matters - answering from stale carried text - is the least tested thing here; a host that wants it sets tool_carry true. Evidence: the fleet manager runs it on deliberately so the live log keeps producing data.",
    "tool_carry": true,
    "tool_carry_chars": 8000,
    "_comment_disclosure": "Tool disclosure. The request carries a small always-visible tool set (the five primitives plus the doors the standing instructions name) and the rest is revealed on demand: find_tools asks for what you want, or the agent simply calls a tool by name and the harness honours it and keeps it for the session. Measured on a live install: the schemas were 2,847 of 5,242 tokens of fixed overhead and 88% of real calls used five tools, so this is the biggest single saving per call. core_tools overrides the visible list; tool_disclosure false sends the whole registry again, which is the rollback.",
    "tool_disclosure": true,
    "core_tools": [],
    "disclosure_max": 4,
    "_comment_atlas": "The machine atlas (item 2b). The harness hands the model the facts about the box it is on - os, shell, install path, and what lives where - because a small model does not look around and then guesses paths (the most common tool error on the graded eval). It rides the TRAILING state block, on the first turn of a run and again after any failure that reads like a wrong path, never in the system prompt. atlas.md is generated on each host at first run and is never shipped in a package, so no host's map travels to another box; hand-written '## notes' in it are the curated half.",
    "_comment_shell_facts": "States this process's rights in the trailing block on the first turn of a run: elevated or not, and what that means for admin-only work. Measured reason: a console build launched from an ordinary terminal on an administrator account hit Access is denied and reported it as a mystery. An account being an administrator is not the same as the process being elevated (UAC gives an admin two tokens).",
    "shell_facts": true,
    "_comment_event_log": "Append one JSONL line per event to sessions/<key>.events.jsonl: run start/end, tool call, tool result WITH its outcome, arguments scrubbed and digested rather than stored raw. SHADOW ONLY - nothing reads it back yet and no prompt or history derives from it, so turning it on changes no behaviour. Off unless you ask for it; the fleet manager runs it to compare the events against tinycmdr.log.",
    "event_log": true,
    "atlas_enabled": true,
    "atlas_file": "atlas.md",
    "atlas_max_chars": 2400,
    "_comment_plan": "The run's plan is held by the HARNESS, not by the model: it is re-sent every turn in the trailing block with the position (calls used and left) and the step statuses. plan_from_request parses a request that lists its own steps into the plan, which is the path that actually gets used: measured, the model never chose to write a plan itself in 19 runs. plan_drift_after is how many tool calls may pass with no step moving before the harness says so; plan_max_steps bounds what a plan may carry; plan_enabled false removes all of it (the eval uses it to measure the feature on one build).",
    "plan_enabled": true,
    "plan_from_request": true,
    "plan_drift_after": 8,
    "plan_max_steps": 24,
    "_comment_notes": "Durable memory. Every 'remember' lands in notes.md, the newest of which rides in the trailing state block, never the system prompt - the system prompt has to stay byte-stable for the endpoint's prefix cache; older entries age into notes-archive.md. notes_max_note_chars caps a single entry at write time, because one huge note poisons every later call in the session.",
    "notes_max_chars": 8000,
    "notes_max_note_chars": 1200,
    "notes_keep_entries": 60,
    "notes_archive_days": 45,
    "_comment_ledger": "The task ledger (tasks.json / tasks.md) is what survives a restart: what this box is in the middle of. tasks_max_open refuses new tasks past that many open ones, so it stays readable.",
    "tasks_max_open": 15,
    "tasks_done_keep": 3,
    "_comment_progress": "Progress must arrive as NEW messages: an edited status line notifies nobody, which is how a real 32-minute run looked like total silence. checkin_minutes and checkin_steps are the two independent cadences (either one posts), 0 turns one off.",
    "progress_updates": true,
    "checkin_minutes": 5,
    "checkin_steps": 30,
    "checkin_per_tool": true,
    "checkin_notes": true,
    "checkin_note_chars": 400,
    "checkin_note_min_seconds": 1.0,
    "checkin_tool_merge_seconds": 2.0,
    "checkin_tool_max_lines": 4,
    "checkin_tool_preview_chars": 90,
    "checkin_tool_min_seconds": 0.0,
    "_comment_stall": "Stall guard. One worker serves a channel, so a wedged run would queue every later message behind it in silence. After stall_warn_minutes with no output it posts a visible 'still on it'; after stall_abandon_minutes it abandons the run and takes the next message.",
    "stall_warn_minutes": 8,
    "stall_abandon_minutes": 20,
    "_comment_loops": "Loop guards for weak models. loop_dedupe_after refuses an identical call (same tool, same args, same output) after that many real runs and hands back the cached result; 0 disables it. loop_stop_repeats forces the final report when a call repeats that many times.",
    "loop_dedupe_after": 2,
    "loop_stop_repeats": 6,
    "_comment_catchup": "A websocket reconnect does not replay what you missed, so the bot sweeps channels it has seen for posts from the gap. catch_up_seconds 0 turns the sweep off; catch_up_max_minutes bounds how far back it will ever reach.",
    "catch_up_seconds": 60,
    "catch_up_max_minutes": 30,
    "_comment_safety": "Two tiers, and neither is a security boundary. blocked_patterns is an ABSOLUTE refusal: nothing approves it in-band, and its refusal text tells the agent to ask you to run the command by hand or to take that pattern out of this list. confirm_patterns is the middle tier: a matching shell command or a matching execute_code source is ASKED about first, and a lane with nobody to ask (a scheduled job, a sub-agent) declines - see confirm_without_door. Setting either key REPLACES the built-in list. Both are matched case-insensitively against the command text and, for execute_code, the source text; code that assembles a command at runtime is invisible to a regex, which is exactly why an over-broad block is worse than a confirm.",
    "blocked_patterns": [
      "rm\\s+-[a-zA-Z]*r[a-zA-Z]*f[a-zA-Z]*\\s+/(?![A-Za-z0-9_./~-])",
      "rm\\s+-[a-zA-Z]*f[a-zA-Z]*r[a-zA-Z]*\\s+/(?![A-Za-z0-9_./~-])",
      "\\bmkfs\\b",
      "\\bdd\\s+.*of=/dev/",
      ":\\(\\)\\s*\\{",
      "\\bshutdown\\b",
      "\\bpoweroff\\b",
      "\\breboot\\b",
      ">\\s*/dev/sd",
      "\\bformat\\s+[a-zA-Z]:",
      "\\b(stop|restart)-computer\\b",
      "\\bformat-volume\\b",
      "\\bclear-disk\\b",
      "\\binitialize-disk\\b",
      "\\bcipher\\s+/w\\b",
      "\\bvssadmin\\s+delete\\s+shadows\\b",
      "-encodedcommand\\b"
    ],
    "confirm_patterns": [
      "\\brd\\s+/s\\b",
      "\\brmdir\\s+/s\\b",
      "\\bdel\\s+/[a-z]*[sq]",
      "\\bremove-item\\b[^|;]*-recurse"
    ],
    "_comment_confirm_door": "confirm_without_door is what a lane with nobody to ask decides: a scheduled job or a sub-agent. decline (the default) skips the command and says so; allow runs it, which is a deliberate choice for a fleet whose jobs are trusted.",
    "confirm_without_door": "decline",
    "_comment_endpoint_tools": "The endpoint gate covers TOOLS as well as shell: a custom tool whose name or description carries one of these markers stops or restarts the model endpoint this bot talks to, so it needs your confirmation - and it is REFUSED outright while the catch-up sweep says posts were lost. Name your own launchers here; nothing is hard-coded per box.",
    "endpoint_tools": ["inferctl", "llamasrv", "serve_", "llama", "vllm"],
    "_comment_ask_user": "ask_user lets a run STOP and ask you a question, then carry on with your answer. Off by default: with it on the model can spend budget parked on a question, so switch it on where a human watches the channel. ask_user_wait_seconds bounds that wait (5..900; the model cannot extend it, and a /stop releases a parked run immediately).",
    "ask_user": true,
    "ask_user_wait_seconds": 120,
    "_comment_extras": "vision: only set true if the endpoint actually accepts images. subagent_model pins delegate_task sub-agents somewhere specific (empty = inherit the conversation's model). show_usage puts the token/time footer on the Done line. debug_dump_dir writes every request body to that directory when troubleshooting a model that answers nonsense (empty = off). announce_restart controls the 'back up' post after /restart.",
    "vision": false,
    "subagent_model": "",
    "show_usage": true,
    "debug_dump_dir": "",
    "announce_restart": true,
    "checkin_stream_notes": true,
    "checkin_stream_seconds": 2.0,
    "color_coded": true,
    "_stream_notes": "When true, the model\u2019s own narration is streamed into ONE message that grows while it writes, so the plan is readable (and stoppable or steerable) before the tools run. False restores posting it once, after the call returns.",
    "auto_continue": true,
    "auto_continue_max": 2,
    "spill_output": true,
    "spill_keep": 50,
    "deliver_after_announcements": 3,
    "session_transcript": true,
    "fetch_max_chars": 12000
  }
}
